# go-botje

Ground-up Go rewrite of botje, BenV's Perl IRC bot from ~2007 that runs as nick **hoer** on the junerules ircd (container Botje-Owl on Uil). End goal: a single Go binary in a minimal Docker image that replaces hoer with functional parity or better.

## Read these before doing anything

1. `docs/architecture.md`: the design + OPEN QUESTIONS. If a decision there is still marked proposal, debate it with BenV before building on it.
2. `docs/roadmap.md`: phase plan and per-module parity checklist. Work top to bottom.
3. `docs/perl-reference/`: the behavioral spec extracted from the Perl source (core-framework.md, modules.md, deployment.md). When in doubt about behavior, this plus the actual Perl source is the truth.

The original Perl tree + live data snapshot is in `reference/` (gitignored, extracted from /docker/botje on Uil). Read it freely; never commit it. The .env and .dat files in there contain live secrets and IRC auth. Do not paste their contents anywhere.

## Hard rules

- **TDD.** Test first, then code. No component lands without tests. Protocol, flood control, formatting, and storage are pure enough for table-driven tests; write them that way.
- **No external IRC/bot frameworks.** The IRC layer is ours (stdlib + crypto/tls). Deliberate choice, same as the Perl bot. Small focused libs for peripheral stuff (sigv4, qr) are fine; discuss anything bigger.
- **Never test against live channels.** Testing happens on the junerules ircd, TLS port (BOTJE_IRC_ADDR in the local .env), channel **#testing** only. Test nick: **Meretrix** (Latin, BenV's pick; if taken, ask BenV). Never connect as hoer, never join #rss or other live channels from test code. Live integration tests are gated behind `BOTJE_LIVE_TEST=1`.
- **Don't touch the running hoer.** No writes to /docker/botje on Uil, no telnet to its 1924, no restarts, unless BenV explicitly asks.
- **No em-dashes** in any output: code comments, docs, commit messages. Plain hyphens, commas, or colons. This is a BenV-wide rule.
- **Commit style:** lowercase imperative, short, like the Perl repo ("fix utf-8 boundary handling in reader"). BenV reviews and pushes; do not push to remotes.

## What botje is (30-second version)

Single-process event-driven IRC bot. Custom select loop, hot-reloadable modules, event bus with per-module hooks, `!` commands with Levenshtein did-you-mean, per-channel round-robin flood control (1 msg/s), 4-line reply pager with `!more`, `{x}` color-tag formatting, per-module key-value storage (Storable .dat files), async HTTP fetcher (incl. AWS SigV4 for Bedrock), telnet admin port 1924. Details in docs/perl-reference/core-framework.md.

17 working active modules to port. The big four: RSS (feed subscriptions per channel, item recall by short id), Markov (learns channel chatter, 29 MB dictionary), Karma (`item++`/`item--`), Ticker (BTC/ETH/stocks with sparklines). Full inventory: docs/perl-reference/modules.md.

## Architecture (locked 2026-07-03)

Single dispatcher goroutine owns module event dispatch (modules stay race-free, like the Perl select loop). Blocking work in goroutines, results re-enter via the event channel. Modules are compiled-in Go packages behind a registry with runtime enable/disable; no dynamic loading. Storage: JSON per namespace, dirty-flag flush every 60s + on shutdown. Package layout in docs/architecture.md.

Carry over verbatim so Perl module logic ports 1:1: event struct shape, command registration semantics (priority/continue default handlers), colorize tag syntax, pager behavior, fetcher option set.

Fix, do not port: RSS short-code collision (the !LI6 incident, see docs/perl-reference/core-framework.md "Known bugs"), hardcoded API keys, flush-on-unload-only persistence, the ChatGPT httpbin debug POST, the telnet eval backdoor.

## Current status (update this section every session)

- 2026-07-03: investigation done, reference docs written, architecture DECIDED with BenV (see docs/architecture.md decisions log): keeper/core process split for reconnect-free upgrades, Postgres storage sidecar, telnet admin 1924, nick Meretrix, search module replaces google scrape (SearXNG), Remind ported, WorkHours dropped, auth reset at cutover.
- 2026-07-03 (later session): Phase 0 + Phase 1 DONE, all TDD, tests green. go.mod (module go-botje), cmd/botje stub, Makefile; internal/sched (timer heap, fake clock); internal/bus (perl submit_event semantics: one hook per module+event, return collection, stop propagation, callchain re-entry refusal, panic force-unload, callstats); internal/conf (typed settings, stored values, file overrides, OnChange); internal/storage (Store interface, memory + postgres/pgx backends, embedded migrations, conformance suite runs postgres via throwaway docker container); internal/format (colorize {x}->mIRC with bold state machine, wrapText 448, TruncateIRC 510 grapheme-safe via rivo/uniseg, sparkline, tfcprint tables). format is GOLDEN-TESTED against the real perl code: internal/format/testdata/golden-gen.pl regenerates golden.json from reference/ (needs the gitignored perl tree). Deliberate divergences from perl documented in format doc comments: wrapText multibyte-join corruption fixed, {i}/{u}/{I} stripped, {n} stripped. Deps: pgx, uniseg.
- 2026-07-03 (same session, later): Phase 2 DONE. internal/irc: ParseLine/ParsePrefix/SplitParams (perl quirks kept: colon-in-middle-param starts trailing, prefixless lines inherit last sender), numerics table generated from the perl source (118 entries, 401 trailing-space bug fixed), LineBuffer framing (partial utf-8 safe, invalid bytes dropped); Session (pure state machine: event construction with query rewrite, channel/topic/mode/motd tracking, PING->PONG high-prio, quit/netsplit classification, ERROR auto-quit) + Backoff (3/60/180/300, stays at 300 instead of the perl fall-off-the-end stop); Conn transport (TLS, reader/writer goroutines, flood-queued colorize+truncate+CRLF writes, single-use, reconnect belongs to owner); irc/flood (per-channel RR, 1/s, 3s cap, >80-byte multi-count; RR rotation per-queue not global); irc/cmd (multi-registration, default handlers, levenshtein suggest incl. annoyance suffix); irc/pager (!more, 600s expiry; fixed: replace cancels old expiry timer). Fixed-not-ported perl bugs documented in code: efPart channel typo, pager stale timer, wrapText corruption. Live smoke test PASSED against junerules #testing as Meretrix (BOTJE_LIVE_TEST=1, internal/irc/live_test.go). Next: Phase 3 (fetch, admin port 1924, auth) then modules.
- 2026-07-03 (same session, later still): Phase 3 partial. internal/fetch done (net/http wrapper: single-flight per url, redirect cap 8 + NoRedirect, 30s default timeout, size limit, basic auth, streaming cb, Options.Sign hook for sigv4 later; callbacks re-enter via injected deliver func -> dispatcher). internal/auth done (bcrypt users in storage ns Auth, persisted per change, config superuser wins over stored users, perl authUser result semantics). Deps added: x/crypto. REMAINING for phase 3: internal/admin (telnet 1924, IAC echo-off during password, 3-fail disconnect with "H-h-h-HACKER!!!", COMMAND event command registration with named-capture regexes, built-ins: help/conf/module/adduser/passwd/irc, NO eval). Then: core assembly (module manager wiring bus+sched+conf+storage+fetch+irc+pager+cmd into botje core/standalone) and Phase 4 modules starting with karma.
- 2026-07-03 (afternoon): make run WORKS (BenV request): internal/core assembles everything into botje standalone (dispatcher loop = work chan + sched timers; reconnect via Backoff; graceful QUIT with the perl quitmsgs list; privmsg = newline split + 448 wrap + flood). Defaults: junerules #testing Meretrix, in-memory storage (BOTJE_PG_DSN=... for postgres). Phase 3 COMPLETE: internal/admin telnet port done (login flow, 3-strikes HACKER disconnect, IAC echo suppression, spec-based commands via COMMAND bus event, builtins help/conf/status/callstats/adduser/passwd/users, su gating, ANSI output; -admin flag default 127.0.0.1:1924; superuser bootstrap via BOTJE_SUPERUSER=name:bcrypt-hash env). Phase 4 modules DONE so far: karma, ego, lastseen, pacman, tinyurl, all wired into standalone, karma verified live end-to-end in #testing (test client got the colorized reply). module.Context is the module API (Bus/Cmd/Pager/Conf/Store/Sched/Fetch/Privmsg). More perl quirks found: lastseen join/part/quit handlers were never hooked (dead code), tinyurl hook raced its own command (fixed with ^! skip), tinyurl API url was unencoded (fixed). Next: rss (the big one, incl. short-code collision fix), pizza (timers), then markov + migration tooling (tools/migrate).
- 2026-07-03 (end of day): 433 nick collision now retries with underscore suffix (perl sat nickless forever; BenV hit this via a leftover test bot process, since killed). tools/migrate DONE for karma: dump.pl (generic Storable->JSON, unblesses DateTime) + go importer (-module karma -in x.json [-dsn ...], dry-run default), PROVEN against the live IRC_Karma.dat: 3889 global items, 3 servers, 16 channels, 4221 channel items, counts double-checked against the raw dump. Live-data fact: 14 channel items have no global entry (predates update-both-sides code), so count equality is the migration invariant, not cross-references. karma.Entry/karma.Data exported for the migrator. NOTE pizza is actually LARGE (896 lines: timespec parser, greek ids, stopwatch), not the quick win the roadmap tiering suggests. Next session: rss (1417 lines) or pizza, both need a full session.
- 2026-07-03 (later): README.md written (BenV request: build/run/admin/migration reference). pizza module PORTED and wired: full timespec parser (absolute h:m:s + d-m-yyyy, relative +/-N units accumulating per unit, weekdays with next-week rule, repeats r{...} with the 1800s FU minimum), greek-letter idPool, stopwatch, clear-all double confirm within 60s, restore with the +20s near-past rule. Divergences documented in the code: stopwatch survives reload as a stopwatch (perl restored it as a broken timer that pinged "nick: " after boot), relative offsets apply largest-unit-first (perl hash order), timers saved after firing, and bare "!pizza +5m" works (perl regex demanded trailing whitespace so specs without a message always failed and the default burning-pizza message was dead code). REMAINING modules: rss, markov, ticker, kind, wiki, urband, wolframalpha, search, remind, llm, nagios. rss is next.
- 2026-07-06: rss module PORTED, wired, and verified live end-to-end in #testing (local http feed served to the bot: add/broadcast with short ids + size extraction/list/bare-id recall with paging/del all worked on the wire). Structure: modules/rss/{ids,parse,rss}.go: idAlloc (base-32 A-Z 0 2 4 6 8 9, wraps 32768, skips command words AND live ids = the !LI6 fix), parseFeed (rss/rdf/atom via encoding/xml, naked-& repair, entity decode + tag strip + colorquotes, pubDate formats with UTC fallback), module (add options refresh>=5/target/tag/grep-with-validation, per-(server,channel) subscriptions with received-guid tracking, broadcast oldest-first capped 25 or 5 for new subscribers, history pruned >10 items and >24h stale, retry 3x60s then 1h, 100-parse-errors abandon, 3-line description paging per channel, restore with 15s stagger). Mutation-checked the test suite (broadcast order, sub cap, retry cadence all caught). Also: 433 nick collision retries with underscore suffix; pg conformance test readiness probe now requires 2 consecutive pg_isready (initdb restart race). Perl quirks NOT ported (documented in doc.go): new-subscriber cap leaked to later subscribers in same round, save only on add/del/unload. NEXT: markov (+ 29 MB dictionary migration, needs its own storage table per architecture.md) or ticker.
- 2026-07-06 (later): markov PORTED + MIGRATED + verified live. Storage decision: trie stays in RAM, persisted as one blob per (order,dict) key in ns markov (kv jsonb handles the 29 MB fine; a real table remains an option later). Learning: sanitize (lc, benv/lotjuh capitalized, punct split, contractions kept whole = fix of the perl $_ bug), windows of order+1 words with per-level counts, closing dot unless EoL/bad-end, save every 51st line (perl post-decrement). Generation: longest-chain walk with the (1/(nkeys+1))^2/4 variety roll (nkeys+1 = perl counting its own __count key), weighted child pick, random-word fallback, >6-words-add-dot, *BLLEUEURRURUHGHG*. only on empty dict, 40-iteration ".…" cap, no-space-before-punct + ucfirst. Defaults: !talk, unknown-!command talk (priority 1 continue 0 = suggestions never fire while markov loaded, live parity), query "talk", idle talker (conf markov_idle_talk/timeout(min)/channels). Migration: markovFromPerl in tools/migrate, real dat = 27614 top words / 465650 total count (3 stray __ top keys skipped), verified by independent recount; full path proven live: dump.pl -> import to throwaway pg -> bot with BOTJE_PG_DSN -> !talk benv in #testing produced vintage hoer ("Benv hoer bloadt skaupa ventiel bram."). Mutation-checked: closing-dot, save threshold, variety roll (dedicated test). NEXT: ticker (1222 lines, alphavantage key must move to conf) or the small API modules (kind/wiki/urband/wolframalpha).
- 2026-07-06 (later still): ticker PORTED and verified live against the real blockchain.info API (subscription, real EUR price broadcast, sparkline graph with {m}-colored eighth-block bars, !btc 4 height, del; note the live API now returns symbol "EUR" not the euro sign, we render whatever it sends). modules/ticker/{sources,ticker}.go: pluggable source table (BTC blockchain.info, ETH kraken XETHZEUR, default alphavantage with key from conf ticker_alphavantage_key + refuse-add-without-key guard), 96-point history, IQR jump detection (values sorted NUMERICALLY, perl string-sorted; the dt-not-abs'd quirk that suppresses drop broadcasts is KEPT for parity, documented), 6h broadcast cap, alarms rise/drop/uprate/downrate with crossing semantics (query copy to nick, perl sent to bogus &nick; the "uprate alarm" text in the downrate message is perl's copy-paste, kept), retry 2x60s then 1h, 10-json-errors abandon with broadcast, restore with rand(30)s offsets. More perl bugs fixed (doc comment): lowercase show failed lookup, add query/#channel options inverted, stock oneliner printed module name instead of symbol, intermediate saves wiped tickerdata. firstSigDigits ports the perl zero-weirdness exactly ("000" for 0). Mutation-checked: broadcast gap, alarm crossing, jump-detection-off (needed a tighter test timeline to actually exercise the jump path). REMAINING modules: kind, wiki, urband, wolframalpha, search, remind, llm, nagios (all small/medium, mostly fetch-based).
- 2026-07-06 (evening): BenV dropped kind, search, nagios from scope. wiki + urband + wolframalpha PORTED and wired; wiki verified live against the real 2026 wikipedia REST endpoint (3 colorized hits in #testing). Keys moved to conf per the no-hardcoded-keys decision: urband_rapidapi_key, wolframalpha_appid (both refuse politely when unset; the old keys are in the perl source if BenV wants to reuse them via telnet conf). Perl quirks kept: urband's doubled color tag on non-first pager lines; wiki's exact spam-brake insult. REMAINING: remind (561 lines, needs a cron engine: recommend robfig/cron/v3, fits the small-focused-libs rule) and llm. LLM JUDGMENT for BenV: recommend porting as the unified module per the locked architecture decision (IRC_ChatGPT is ACTIVE on live hoer, so parity needs it; fetch.Options.Sign hook is ready for bedrock sigv4). Plan: start from IRC_ChatGPT behavior (425 lines) with an openai backend + conf-based key and per-channel history, add bedrock/ollama backends behind the same interface after. Both remind and llm want their own session.
- 2026-07-06 (night): remind + llm DONE. All in-scope Phase 4 modules complete (kind/search/nagios dropped by BenV). remind: robfig/cron/v3 for schedule math (su->0 not perl's 7), cron reminders with day/month name shorthands + show/clear, remember/recall/forget notepad (last 3 values per name, 4-level server/channel/nick/title nesting kept for future migration; ANSI colors verbatim). llm: UNIFIED module per architecture decision - one module, Backend interface, three impls (openAI !gpt, bedrock !claude, ollama !oi), shared per-channel history (16-turn cap) + fetcher single-flight. ALL keys in conf (llm_openai_key, llm_aws_key/secret/session/region, llm_bedrock_models allow-list, llm_ollama_url/model); httpbin debug POST dropped; hoer persona = the newer bedrock system prompt (not the DAN/evil jailbreaks). SIGV4: wrote internal/fetch/sigv4.go from scratch (no aws-sdk, keeps deps small per architecture) - VERIFIED OFFLINE against the official AWS get-vanilla test vector (sig 5fa00fa3...) so bedrock is correct even though BenV can't test AWS right now (his account is down post Binx->Xebia); bedrock signs via fetch.Options.Sign. BenV just needs to set llm_aws_key/secret via telnet conf when AWS is back. Could not live-test llm (no keys) - all other modules were live-verified, llm is unit-tested only incl. the sigv4 known-answer. Mutation-checked remind (reschedule, stack cap) and llm (history cap, context reduction). Deps added: robfig/cron/v3. NEXT: Phase 5 - Dockerfile + compose, keeper/core split (currently standalone only), parallel run, cutover. Also pending: migration transformers for the modules beyond karma/markov (ego/lastseen/rss/ticker/pizza/remind data) when needed.
- 2026-07-06 (Phase 5 start): Dockerfile + compose DONE and verified live (stack up, Meretrix in #testing over the pinned junerules host, all 14 modules loaded, postgres auto-migrated, health green). Dockerfile = alpine two-stage, 21 MB static binary (CGO_ENABLED=0), tzdata+ca-certs, nonroot. compose = bot + postgres sidecar, BOTJE_PG_DSN wired, admin published on 127.0.0.1:1924 (bot binds 0.0.0.0 inside, host publish restricts), extra_hosts pins the ircd FQDN to its LAN address, .env for POSTGRES_PASSWORD/BOTJE_SUPERUSER, .env.example committed. DEFAULTS ARE SAFE (Meretrix/#testing); cutover = change command nick/channels + migrate fresh dump. Migration: ego + rss transformers added as exported MigrateFromPerl in their packages (type-safe, tested readable-back-through-module), wired into tools/migrate. dump.pl now aliases the dropped Europe/Amsterdam DateTime tz to Brussels (BEGIN block) so rss/lastseen DateTime objects thaw - epoch is tz-independent so correct. Real-data proven: karma 3889 global, markov 27614 words, ego 873 nicks, rss 25 feeds/2959 items. DECIDED not to migrate ticker/remind/pizza/lastseen: pizza .dat is empty, lastseen is 2017-stale + only-on-unload, ticker is a couple BTC/ETH alarm subs, remind is one 2016 cron - all trivially recreated at cutover (matches the auth-reset-at-cutover philosophy). NEXT phase-5 remaining: keeper/core process split (currently standalone only - reconnect-free upgrades; standalone-with-restart works as the accepted fallback), parallel run (deploy compose alongside hoer), cutover, keep perl image for rollback. The keeper/core split is the one real architecture piece still unbuilt.
- 2026-07-06 (keeper/core DONE): internal/keeper = the connection-keeper process. Transparent buffering BYTE relay (no parsing/flood/session logic per architecture "keeper stays dumb"): owns the IRC TCP/TLS socket, relays raw bytes to/from core over a unix socket, buffers inbound (bounded 4MiB) while core is away, reconnects IRC with irc.Backoff. cmd/botje: `keeper` (-addr/-tls/-socket) + `core` (-socket, connects via unix socket; core.Config.Dial does net.Dial("unix",...)). KEY INSIGHT: the relay is byte-identical to a direct IRC connection, so core's existing irc.Conn works unchanged through it. RECONNECT-FREE UPGRADE works via core.Config.SkipGoodbye: a core under a keeper does NOT send the QUIT on shutdown (that would drop the session); the keeper owns the real goodbye QUIT on its own stop. Bug found+fixed during live test: without SkipGoodbye, core SIGINT sent QUIT -> keeper relayed -> ircd dropped us -> keeper reconnected (defeating the point). VERIFIED LIVE on junerules: keeper logged exactly ONE 'connected to IRC' and NO reconnect across a full core kill+restart; core2 resumed through the same keeper (re-NICK/re-JOIN are ircd no-ops). compose gained a "split" profile (keeper+core+postgres, shared keepersock volume); default profile stays standalone. `docker compose --profile split up -d` then `docker compose restart core` = reconnect-free module/dispatcher upgrade. Phase 5 REMAINING is all BenV-action: parallel run (deploy compose alongside hoer), cutover (stop hoer, fresh dump+migrate, take nick hoer), keep perl image for rollback. Everything buildable is built: all in-scope modules, deploy artifacts, migration for the 4 modules that matter, keeper/core split. Test suite: 30 packages green.
- 2026-07-03 (admin bootstrap, BenV request): no default telnet credentials by design. Bootstrap paths: BOTJE_SUPERUSER=name:password (plaintext hashed at boot, dev) or name:bcrypt-hash (from 'botje hash <pass>'); 'botje adduser <user> <pass>' upserts a user directly in postgres storage (refuses without BOTJE_PG_DSN, in-memory users die with the process). auth.ParseSuperuser tested. All verified live incl. telnet login as bootstrapped superuser and adduser insert+update against throwaway pg. Telnet prompt confirmed working with a real telnet client; note the prompts have no trailing newline, so line-buffered viewers show nothing until you type.
- 2026-07-07 (Uil deploy blockers, TODO items 1-5 DONE): security scrub (repo is public at github.com/benv666/botje): no internal FQDN/RFC1918 IPs anywhere in tracked files, verified with git ls-files grep; flag defaults now come from BOTJE_* env (BOTJE_IRC_ADDR is REQUIRED, no in-code server default; live tests read it from env). docker-compose rewritten: keeper+core+postgres is THE mode (no profiles, plain up -d), keeper runs a pinned image tag from .env (BOTJE_KEEPER_IMAGE, no build section -> core rebuilds never recreate it, make keeper-image builds+tags one), all site config in .env (.env.example documents everything incl. BOTJE_EXTRA_HOST fqdn pin and BOTJE_PG_DATA bind mount), compose file is static. Makefile run/livetest source .env in-shell (make itself eats # in BOTJE_CHANNELS as a comment - documented there). ticker migration transformer done (modules/ticker/migrate.go MigrateFromPerl, wired into tools/migrate): perl string alarms -> float64, runtime fetcher subtree dropped, data points pass through raw with points-without-time dropped; proven on the real IRC_Ticker.dat: 2 tickers, 2 subs, 192 points, 8 alarms, plus readable-back-through-module test (!ticker list + restore polling). Suite green (30 pkgs). REMAINING: TODO item 6 (channel persistence + telnet join/part + invite auto-join), then cutover (BenV action).
- 2026-07-08 (evening: pacman fancy + keeper-resume BUGFIX): pacman got a 6th template, a fancy UTF-8 pac-man (░▟▀▙ / ▓█▝◣ ● ● ● nick / ░▜▄▛: shaded soft edge, rounded disc, eye ▝, wedge mouth ◣). Enabled by first fixing core.privmsg newline handling: it split on /\n\s*/ (perl cmd_privmsg parity) which ATE leading whitespace on every continuation line and misaligned all multi-line ASCII art - now splits on \n only + drops blank lines (perl's own cmd_eventmsg behavior), so art keeps its margins. LIVE BUG found by BenV after a core rebuild: the un-joined-channel flood guard dropped messages to channels the bot was clearly in. Root cause = keeper/core state: a core restarting under the keeper re-JOINs channels the live session is already in, ircd sends NO join echo for already-joined channels (logged ERR_ALREADYREGISTRED), so the fresh core's session.channels stayed empty and InChannel was false for everything. Fix: session.JoinChannels marks membership optimistically when sending JOIN (echo just refreshes). Known minor: ~10s post-restart window before JoinChannels runs where a resumed core still drops. All committed, needs core rebuild on Uil to land.
- 2026-07-08 (metrics DONE, backlog cleared): internal/metrics = hand-rolled prometheus text exposition (counters+gauges, scrape-time collectors, label escaping/sorting; NO client_golang, same few-deps choice as sigv4). core.Config.Metrics/MetricsAddr optional; collector dumps bus callstats (botje_hook_calls_total{module,event}, botje_hook_duration_seconds_sum) + botje_connected/botje_modules gauges + botje_reconnects_total; admin.Server.Metric callback (kept metrics OUT of the admin package's imports) bumps botje_admin_logins_total{result}. Flag -metrics/BOTJE_METRICS (empty=off); compose runs core with -metrics=0.0.0.0:9095 published on 127.0.0.1:9095 (loopback, like admin). Point Uil's prometheus at it. Reminder: botje_hook_calls_total is BUS HOOKS only, not the command registry (echoModule has no hook = no series; test uses a hook module). ALL of today's backlog (guard stages 1-3, flood guard, metrics) DONE; remaining backlog = refactors (markov save amplification first: measure Put latency on Uil, then dirty-flag+60s flush or per-top-word rows).
- 2026-07-13 (OUTAGE fix: keeper reconnect was not session-aware): drydock updated inspircd ~08:30, Meretrix never came back. Chain (all evidenced in keeper log + inspircd security.log): keeper silently reconnected the IRC socket but the attached core was never told -> nothing re-sent NICK/USER -> ircd dropped the unregistered conn every ~11s -> keeper redialed INSTANTLY (backoff only ran on dial failure, not on short-lived success) -> 17 connects/3min -> m_connectban Z-line on the docker bridge IP (EOF on every dial, 1h) -> and irc.Backoff's quiet-period reset compared against Next's CALL time, so its own 300s sleep reset the ladder to 3s (cycled 3s/1m/3m/5m forever, re-tripping the ban after each expiry). FIXES (TDD, mutation-checked, suite green): (1) Backoff.when now records the scheduled attempt (call+delay), only real peace >300s past the attempt resets; (2) keeper dropSession on IRC loss: closes the core conn (core's existing reconnect re-registers, NICK/USER stay core-owned = keeper stays dumb) + discards stale inbound buffer; (3) keeper consults backoff after EVERY connection loss; (4) outbound from a core that re-attaches while IRC is down is buffered (16KiB cap, overflow keeps the head at a line boundary - first version dropped the whole chunk when one Read exceeded the cap, caught by 3/5 flaky test runs) and flushed on IRC connect, so registration survives either ordering; (5) pumpIRC read deadline: 5min inbound silence = dead conn (Config.ReadTimeout). Test gotcha: gated-sleep tests must wait for the keeper to actually reach the reconnect sleep before attaching a core, else dropSession races the fresh attach. NOT DEPLOYED: permission classifier blocked ssh docker/git on Uil; BenV must build+deploy both images (keeper changed! new pinned tag) - commits 5a34589 + 21c9440 local only. IRCD SIDE for BenV: bot IP + ::1 need a connect class with useconnectban=no (the ::1 healthcheck Z-line cycle from 2026-07-08 still churns hourly; the bot Z-line self-sustained the outage).
- 2026-07-08 (guard stage 3 enforcement + core plumbing DONE): module.Context gained SendRaw (high-prio raw IRC line via conn.WriteHigh, for oper commands that must not queue behind flood chatter) + InChannel(chan)bool (session membership). core.privmsg now DROPS channel messages to channels the bot is not in (isChannel + session.InChannel) - the !koffie? flood-budget bug, fixed; queries unaffected; 3 old core tests needed a JOIN-echo added since they replied to #testing without simulating the join. Guard enforce.go: while enabled, non-resident masks tracked as suspects (in-mem, cleared on 'guard off'); heuristics = line rate (guard_line_rate/window_sec), cross-channel duplicate (guard_dup_channels, approximated as identical-text-count capped by shared-channel-count), mass-join (guard_join_channels/window_sec); trip -> GLINE *@host guard_gline_seconds + KICK from every shared channel, once per suspect (actioned flag). Escape hatch: /msg bot 'auth <guard_auth_password>' promotes to resident (fail-closed if password unset). status shows enforced count. Mutation-checked (resident-immunity, actioned-once, auth-promote). Gotcha during tests: nil ctx.Privmsg in a fixture -> bus recovers the panic and FORCE-UNLOADS the module (real behavior), looked like a logic bug. NEXT backlog: metrics (prometheus), then refactors (markov save amplification first).
- 2026-07-08 (guard stage 2, skeleton DONE): modules/guard, autoloaded, idles by default. Residents table: user@host -> lastSeen (NOT nick!user@host: nick changes must not reset residency), learned from PRIVMSG+JOIN while guard is OFF, persisted ns guard key residents (map[server]map[mask]epoch, 5min dirty flush + flush on QUIT event + Unload), lastSeen advances throttled to 1/h, aged out after conf guard_resident_days (90). Telnet: guard on|off|status (su; toggle = conf guard_enabled so it persists via core conf storage). While ON: trust set FROZEN (spam wave cannot become resident), non-resident joins counted + slog'd to ops.log ('guard: non-resident join') = observability before enforcement. Mutation-checked (freeze, aging, throttle all caught). Live gotchas found by BenV opering: v4 <type modes=> needs the opermodes module (not loaded -> Meretrix had no +O; BenV loading it + keeper restart pending), snomasks now need snomasks= grant on an oper <class>. NEXT guard stage 3: enforcement - needs module raw-IRC send (module.Context, decide flood-queue vs WriteHigh), spam heuristics (cross-channel duplicate lines, mass-join, line rate), oper kickban + timed GLINE, auth flow for mid-wave newcomers.
- 2026-07-08 (certfp live + fixes): AUTO-OPER VERIFIED LIVE: whois shows oper type Bot + matching sha256 certfp (the 'old client certificate fingerprint' whois line is sslinfo's legacy hash, harmless). Gotcha found via whois: type modes= needs the opermodes MODULE (not loaded on junerules), so +sO never applied - override is dead until BenV loads m_opermodes.so and the bot re-opers (keeper restart = reconnect + autologin). Same reason BenV's own NetAdmin modes did nothing. Also: filter engine glob needs m_regex_glob.so (was disabled); connectban z-lined ::1 (the ircd container's own health check, ~60 connects/h > threshold 6) - fix is a localhost connect class with useconnectban=no; letsencrypt certs root-owned vs ircd uid 10000 = renewal timebomb, deploy-hook copy recommended. Bot-side fix: admin connection log line demoted to debug (compose health check dialed 1924 every 30s and spammed ops.log; login attempts still info/warn).
- 2026-07-08 (later, guard stage 1: certfp plumbing DONE): irc.ClientTLS(cert,key) helper (nil for empty pair, error on half a pair, handshake-tested against a client-cert-requiring server); keeper.Config + irc.ConnConfig + core.Config carry CertFile/KeyFile; flags -tls-cert/-tls-key (BOTJE_TLS_CERT/KEY) on keeper and standalone; compose mounts ./mounts/certs:ro into the keeper. Cert GENERATED on Uil (/docker/botje-go/mounts/certs/meretrix.{crt,key}, 10y self-signed CN=Meretrix), sha256 fingerprint 3503be049f60fa341ffe2f22bc16251ae14cc4292e0c60937db493d6ff8a035e; BenV must sudo chown 1000 the pair (key is 600 benv-owned, container cannot read it) and add the oper block + Bot type to his opers.conf (given in chat: autologin=strict nopassword fingerprint, type Bot = BanControl+SACommands, override list, modes +sO - the O is MANDATORY for override, learned 2026-07-07). inspircd facts verified this session: v4 RELOADS newly-added config modules on plain /rehash (connectban+filter loaded that way live); v4 log levels are rawio/debug/normal/warning/critical (his level=default is not one); no strftime tokens in log targets, rotation = logrotate copytruncate. His node now runs connectban + filter (glob engine, /FILTER at spam time, actions gline/zline propagate network-wide). NEXT guard stages: guard module skeleton (toggle + residents table), then detection+enforcement.
- 2026-07-08 (backlog items 1+2 DONE): logger + example modules, admin audit. NEW EVENT IRC_SENT: core.privmsg submits one event per outbound line (separate event, NOT IRC_PRIVMSG+SenderMe, so markov cannot learn bot output; nested Submit is safe, bus chain tracking is per module+event). format.Strip added (tags + mIRC + ANSI removal). internal/teelog: slog tee handler, BOTJE_LOG_DIR/ops.log next to stderr, wired in runCore. internal/admin now audits: connection, login ok/failed with addr, 3-strike disconnect, executed commands BY SPEC NAME ONLY (raw line may hold passwords - test asserts no leak). modules/logger (autoloaded): per-channel daily files <network>/<#chan>/YYYY-MM-DD.log, queries/<nick>/, server/ for channel-less events (quits, own modes, server notices: dotted sender or * target); irssi-ish lines, ACTION rendered, other CTCP dropped; conf logger_dir (default $BOTJE_LOG_DIR, empty=off), path traversal sanitized. modules/example: commented full-API skeleton (cmd, default handler, bus hook, conf, storage, sched, fetch, pager, COMMAND admin spec), NOT autoloaded, its test keeps it honest incl. unload deregistration. Compose mounts ./mounts/logs:/logs + BOTJE_LOG_DIR (host dir needs chown 1000, README'd). VERIFIED LIVE locally: channel log caught own join, ops.log caught boot+nick-in-use, server notices routed after fix. Backlog remaining: flood guard (3), metrics (4, added today per BenV: prometheus endpoint + grafana), refactors (5).
- 2026-07-07 (later, TODO item 6 DONE): channel persistence + runtime channel management + conf persistence. Storage ns core: key "channels" = the autojoin set ([]string, case-insensitive dedup), key "conf" = runtime-set conf values (map[string]string). BOTJE_CHANNELS/-channels only SEEDS the set on first boot (empty db); after that storage wins, so .env channel edits do nothing once seeded - use telnet. New telnet builtins join <chan> / part <chan> (su, persist + send JOIN/PART; part of unknown channel errors). IRC_INVITE hook in core joins + persists (perl parity: IRC_Test inviteHandler, but persisted which perl never did). conf.Set now records into the stored map and core saves it on every OnChange + loads before module Create calls, so telnet conf changes (llm keys!) finally survive restarts - previously they silently died with the process. Conf.Stored() keeps unclaimed loaded values (module not loaded this run) and never leaks defaults, so changed defaults still apply to untouched installs. All TDD (5 new tests incl. two-run persistence + telnet join/part over the pipe harness), mutation-checked (invite-no-persist, always-reseed, set-not-stored all caught). NOT live-verified: invite needs a second op client and the #testing-only rule makes that awkward; BenV can /invite Meretrix somewhere as the live check. Suite green. TODO list ALL DONE - remaining work is cutover itself (BenV action) per the phase 5 plan.

## TODO for next iteration (BenV, 2026-07-06, Uil deploy blockers)

1. [x] pgdata volume: bind-mount friendly (BenV uses /docker/botje-go/mounts/postgres/var/lib/postgresql/data on Uil).
2. [x] Move server/nick/channels/etc to .env; docker-compose as static as possible.
3. [x] Split mode becomes the DEFAULT compose mode, no profiles: plain `docker compose up -d`. Keeper must NOT be recreated when core is rebuilt -> keeper gets a long-lived pinned image tag from .env (e.g. botje:keeper-<date>), core keeps build+latest.
4. [x] SECURITY: repo is now public (github.com/benv666/botje). Remove the internal FQDN (irc.benv.*) and RFC1918 IPs from all committed files: flag defaults in cmd/botje/main.go, compose extra_hosts, docs/, CLAUDE.md itself, test data, live_test target (env-driven). Git history has sailed, but HEAD must be clean. Be mindful of this in all future commits.
5. [x] Ticker data migration transformer (tickers + tickerdata).
6. [x] (carried over) channel persistence + telnet join/part + invite auto-join: channels are currently flag-only, reset every boot, no runtime management.

## TODO backlog (BenV, 2026-07-07): logging, example module, refactors

Planned with BenV, NOT started. Discuss open design points before building.

1. [x] Logger module: per-channel IRC logs + global ops log, written to a mounted folder.
   - NOT a port: perl Log.pm was console/debug plumbing (named "log channels" to stdout); its per-IRC-channel file logging stayed a TODO from 2007 until the end. This is new capability.
   - Per-channel logs: modules/logger hooks IRC_PRIVMSG/NOTICE/JOIN/PART/QUIT/KICK/MODE/TOPIC/INVITE, writes irssi-ish plain text to BOTJE_LOG_DIR/<network>/<channel>/YYYY-MM-DD.log (daily files = rotation for free). PREREQ core change: outbound messages emit no bus event today, so the bot's own lines would be missing; core.privmsg should submit the event with SenderMe set (perl parity question: hoer's sends did not re-enter the bus either, decide with BenV).
   - Ops log: admin telnet connect/login success/login failure with remote addr, executed su commands, conf changes, module load errors, connect/reconnect, invite/join/part. Today this is slog-to-stderr only (docker logs). Recommended shape: a tee slog.Handler in core writing (leveled, no color) to BOTJE_LOG_DIR/ops.log alongside stderr, plus ADDING the missing slog calls in internal/admin (login attempts currently log nothing - BenV explicitly wants auth audit). Alternative shape (bus events ADMIN_LOGIN etc consumed by the logger module) is cleaner conceptually but more moving parts; decide.
   - Deploy: compose mounts ./mounts/logs:/logs, BOTJE_LOG_DIR=/logs, .env.example entry. mounts/ already dockerignored. Nonroot uid 1000 must own the host dir (same lesson as the keepersock volume).
   - Tests: formatting table-driven, day-rollover with fake clock, writes into t.TempDir.
2. [x] Example module: modules/example, a heavily commented skeleton exercising the full module.Context surface (command + default handler, bus hook, conf setting with stored value, storage round-trip, sched timer, fetch callback, pager reply, COMMAND admin spec). Perl equivalent was IRC_Test.pm (autoloaded; its invite handler moved into core). NOT in the autoload list, but kept compiling and loadable by its own test so it cannot rot. README pointer "writing a module: start here".
3. [x] Flood guard for un-joined channels: RSS/ticker broadcasts to a channel the bot is not in burn the shared 1 msg/s flood budget on ERR_CANNOTSENDTOCHAN rejects and delay real replies (diagnosed live 2026-07-07, 59 rejects/30min while #rss was pending invite). Fix-not-port: drop (or hold+log) channel-targeted privmsgs when the session is not in that channel; queries to nicks unaffected. Small, do it together with the logger so drops are visible in the ops log.
4. [x] Metrics (BenV, 2026-07-08): prometheus endpoint on the bot for grafana - counters/gauges worth having: events dispatched per type, per-module hook timings (bus callstats already collects this), flood queue depths, storage put latency (feeds the markov refactor decision), fetch counts/errors, reconnects, admin logins. Shape: promhttp on a loopback port published like the admin port, scraped by the existing prometheus on Uil. Check what inspircd exposes too (v4 has no native prometheus; a channel/user-count poller via the bot could cover it).
5. [x] Guard module (spam gatekeeper), DONE (stages 1-3), 2026-07-08:
   - Context: junerules gets occasional drive-by spam waves (join all channels, dump lines, leave). Network is 3 linked inspircd nodes: BenV's (benv.junerules.com) + 2 run by Verty; BenV cannot adhoc-edit Verty's configs, so enforcement must prefer network-propagating mechanisms: kicks, +b bans, G/ZLINEs (xlines sync) - NOT muteban/repeat channel modes unless Verty loads them too (one-time ask, optional stage).
   - DECIDED: no Anope (rejected, too much hassle). Meretrix IS the services. Auto-oper at connect: oper block on BenV's node with certfp autologin (inspircd v4 <oper autologin="strict" fingerprint=...>); the KEEPER owns the TLS socket, so client cert support goes in keeper.Config (BOTJE_TLS_CERT/KEY env). Fallback: conf-stored oper password, core sends OPER.
   - DECIDED: toggle. Guard idles by default and acts only when switched on (telnet guard on|off|status, or conf guard_enabled) - BenV flips it when spammers show up. While idle it should still passively build the residents table (cheap, makes toggle-on instantly useful).
   - Residents: masks (nick!user@host) seen joining/talking during normal operation, aged out after N months. When guard is ON: non-resident joiners get watched; spam heuristics (same line in >=2 channels, >=N lines in T sec, host joining >=X channels in Y sec) -> kickban everywhere (bot is oper, override kick) + timed GLINE (propagates to Verty's nodes). Legit newcomers during a wave: query Meretrix "auth" flow (perl IRC_Auth precedent) or BenV whitelists via telnet.
   - Optional later stage (needs Verty to load muteban network-wide): quarantine mode, +b m:mask on unknown joiners until authed, zero spam window.
   - Open: exact thresholds; whether residents auto-trust replaces password auth entirely.
6. [ ] Refactor candidates, ranked (plan only):
   a. Markov save amplification: every 51st learned line rewrites the whole ~8.3 MB trie blob to postgres, synchronously on the dispatcher. Heaviest write in the bot, will only grow. Measure the Put latency on Uil first, then either dirty-flag + 60s flush (what architecture.md always wanted), or split the trie over per-top-word rows in a real table. Perl had the same save-every-51-lines cadence but wrote a local Storable file, so parity is not an argument for keeping the blob.
   b. Central dirty-flag/flush layer in storage (Saver: MarkDirty + periodic flush + flush on unload); modules currently hand-roll save-on-change (ticker per fetch, rss on add/del, markov per 51 lines). Subsumes half of (a); do (a) and (b) as one design.
   c. internal/core/core.go split: dispatcher, transport/reconnect, admin builtins, channel set, and privmsg pipeline all live in one ~600-line file now. Split into core.go / transport.go / builtins.go. Same session: dedupe the telnet test harness that is copy-pasted 3x in core_test.go (extract adminHarness).
   d. Migration transformer placement: karma+markov transform in tools/migrate, ego/rss/ticker as modules/*/migrate.go. Unify on module-owned MigrateFromPerl.
   e. cmd/botje/main.go: standalone/core/keeper duplicate the flag->Config wiring; fold into one shared flag helper.
   f. (post-parity, big) bus.Event.Extra map[string]any stringly-typed access everywhere; promote the hot keys to typed fields once perl-parity pressure is gone. Touches every module, do last.

## Environment notes

- Go toolchain: check `go version` locally before assuming.
- The junerules ircd is BenV's own; address lives in the gitignored .env (BOTJE_IRC_ADDR). TLS verified working (TLS 1.3, wildcard cert).
- Data migration source: reference/mounts/data/*.dat (Perl Storable). Migration plan in docs/architecture.md.
- Prod deployment target mirrors current: compose on Uil, /data volume, loopback admin port. See docs/perl-reference/deployment.md.
- Editing this file from a benv-brain session: the vault's protect-system-files hook blocks any Write to a file whose basename is CLAUDE.md (over-broad basename match; it means to protect only the vault dispatcher). Run go-botje work in its own session from ~/code/go-botje.
